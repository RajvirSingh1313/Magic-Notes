// Importing [blank] from React in order to [blank]
import {} from "react";

export default function ButtonOpacity(props) {
    // Initalizing variables for useState
    const [touched, setTouched] = React.useState(false);
}